# Log cbf_sensor_dashboard messages to file
if $programname == 'cbf_sensor_dashboard' then /var/log/cbf_sensor_dashboard.log
# Uncomment the following to stop logging anything that matches the last rule.
& ~
